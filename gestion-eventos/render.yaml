services:
  - type: web
    name: el-aleph-system
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/gestion-eventos-0.0.1-SNAPSHOT.jar
    plan: free